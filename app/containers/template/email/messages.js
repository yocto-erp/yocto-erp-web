import { defineMessages } from 'react-intl';

export const scope = 'app.containers.template_email';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Mẫu Email',
  },
});
