import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VerifyMail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Verify Email',
  },
});
